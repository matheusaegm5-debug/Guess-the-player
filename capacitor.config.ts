import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.guesstheplayer.app',
  appName: 'Guess the Player',
  webDir: 'dist',
};

export default config;
